{
  "private": true,
  "name": "jester-packages",
  "scripts": {
    "postinstall": "lerna bootstrap --stream",
    "build": "lerna run build --stream",
    "clean": "lerna run clean",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "lerna": "^2.1.2"
  }
}
